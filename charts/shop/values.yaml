# Default values (this baseline acts as the "dev" environment).
global:
  imageRepo: 822800
  imageTag: v1

frontend:
  replicas: 1

productsApi:
  replicas: 1

ordersApi:
  replicas: 1

gateway:
  nodePort: 30080

# Reserved for Lab 4 (private registry). Empty = public images, no pull secret.
imagePullSecrets: []  
